<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="_webgoodsqna">

    <sql id="dynamicWhere">
    
          <!-- 판매자 -->
          <if test="q_userId != null  and q_userId != ''">
              AND B.USER_ID = #{q_userId}
          </if>
          
           <if test="q_registId != null  and q_registId != ''">
              AND A.INQR_ID = #{q_registId}
          </if>
        
         <!-- 검색어 -->
         <if test="q_searchVal != null  and q_searchVal != ''">
            <!-- 문의제목 -->
            <if test='q_searchKey == "1001"'>
                AND A.INQRY_SJ LIKE '%' || #{q_searchVal} || '%'
            </if>
            <!-- 문의자 -->
            <if test='q_searchKey == "1002"'>
                AND C.USER_NM LIKE '%' || #{q_searchVal} || '%'
            </if>
            <!-- 서비스명 -->
            <if test='q_searchKey == "1003"'>
                AND B.GOODS_NM  LIKE '%' || #{q_searchVal} || '%'
            </if>
         </if>
        
         <!-- 날짜 -->
         <if test="q_beginDate != null  and q_beginDate != ''">
            AND TO_CHAR(A.REGIST_DT,'YYYYMMDD') BETWEEN REPLACE(#{q_beginDate},'-','') AND REPLACE(#{q_endDate},'-','')
         </if>
         
         <!-- 상태 -->
         <if test="q_answerSttusCd != null  and q_answerSttusCd != ''">
            AND A.ANSWER_STTUS_CD = #{q_answerSttusCd} 
         </if>
         
         <!-- 언어 -->
         <if test="q_langCodes != null  and q_langCodes != ''">
             AND B.LANG_CODE
             <foreach collection="q_langCodes" item="item" index="index" separator="," open="IN (" close=") ">
                TRIM(#{item})
             </foreach>
         </if>
         
         <!-- 구분 -->
         <if test="q_goodsTyCds != null  and q_goodsTyCds != ''">
             AND B.GOODS_CODE IN (
                    SELECT GOODS_CODE  FROM TST_GOOD_CATE_I WHERE CTGRY_CODE IN (
                    SELECT CTGRY_CODE FROM TST_CATE_MGMT_M WHERE CTGRY_CL_CD
             <foreach collection="q_goodsTyCds" item="item" index="index" separator="," open="IN (" close=") ">
                TRIM(#{item})
             </foreach>
             ))
         </if>
        
    </sql>
     
    <sql id="dynaminLangCode">
        <choose>
            <when test="langCd != null and langCd != ''"> #{langCd}</when>
            <when test="q_langCd != null and q_langCd != ''"> #{q_langCd}</when>
            <otherwise>'00'</otherwise>
        </choose>
    </sql>
     
    
    <select id="qnaList" parameterType="map" resultType="webGoodsQnAVO">
    
        SELECT 
               A.GOODS_NM AS goodsNm
             , A.LANG_CODE AS langCode
             , B.LANG_NM    AS langNm
             , A.GOODS_CODE AS goodsCode
             , A.GOODS_INQRY_NO AS goodsInqryNo
             , A.INQRY_SJ   AS inqrySj
             , A.INQR_ID    AS inqrId
             , A.USER_NM    AS inqrNm
             , A.ANSWER_STTUS_CD AS answerSttusCd
             , A.OTHBC_AT AS othbcAt
             , TO_CHAR(A.REGIST_DT,'YYYY-MM-DD HH24:MI') AS inqryDt
             , TO_CHAR(A.ANSWER_DT,'YYYY-MM-DD HH24:MI') AS answerDt
             , (
                    SELECT 
                         GROUP_CONCAT(DISTINCT BB.INDVDLZ_CD_NM ORDER BY BB.INDVDLZ_CD_NM DESC  SEPARATOR ' <![CDATA[<br/>]]> '  ) 
                    FROM TST_CATE_MGMT_M AA, OP_CODE_INDVDLZ BB
                    WHERE CTGRY_CL_CD = BB.INDVDLZ_CD
                    AND BB.GROUP_CD = '1005'
                    AND BB.LANG_CODE = <include refid="dynaminLangCode"/>
                    AND CTGRY_CODE IN ( SELECT CTGRY_CODE FROM TST_GOOD_CATE_I WHERE GOODS_CODE = A.GOODS_CODE)
                
               ) AS ctgryClNm
         FROM 
                (
                
                   SELECT Y.* FROM (
                      SELECT ROWNUM NUM, X.* FROM (
                  
                      SELECT 
                             B.GOODS_NM
                           , B.LANG_CODE
                           , A.GOODS_CODE
                           , A.GOODS_INQRY_NO
                           , A.INQRY_SJ
                           , A.INQR_ID
                           , C.USER_NM
                           , A.ANSWER_STTUS_CD
                           , A.OTHBC_AT
                           , A.REGIST_DT
                           , A.ANSWER_DT
                        FROM TST_GOOD_INQR_I A , TST_GOOD_INFO_I B, OP_USER C
                       WHERE B.GOODS_CODE = A.GOODS_CODE
                         AND C.USER_ID = A.INQR_ID
                       <include refid="dynamicWhere"/>
                       
                        ) X WHERE ROWNUM &lt;= #{pagingEndNum}
                   ) Y WHERE NUM &gt;= #{pagingStartNum}
     
                ) A ,(
                    /* 언어 */
                     SELECT
                            LANG_CODE
                          , LANG_NM 
                       FROM TCO_SVC_LANG_C
                      WHERE DOMAIN_CD IS NOT NULL
                ) B
        WHERE B.LANG_CODE = A.LANG_CODE
    
    </select>
    
    <select id="qnaCount" parameterType="map" resultType="int">
        SELECT 
               COUNT(1)
          FROM TST_GOOD_INQR_I A , TST_GOOD_INFO_I B , OP_USER C
         WHERE B.GOODS_CODE = A.GOODS_CODE
           AND C.USER_ID = A.INQR_ID
         <include refid="dynamicWhere"/>
    </select>

    <select id="commentView" parameterType="webGoodsQnAVO" resultType="webGoodsQnAVO">
        SELECT 
               A.GOODS_INQRY_NO AS goodsInqryNo
             , A.INQR_ID    AS inqrId
             , B.USER_NM    AS inqrNm
             , A.INQRY_SJ   AS inqrySj
             , A.INQRY_CN   AS inqryCn
             , A.INQRY_ANSWER_CN  AS inqryAnswerCn
             , DECRYPT(A.ANSWER_RECPTN_EMAIL,'P008')     AS answerRecptnEmail
             , TO_CHAR(A.REGIST_DT,'YYYY-MM-DD HH24:MI') AS inqryDt
             , A.ANSWER_STTUS_CD    answerSttusCd
             , A.OTHBC_AT   othbcAt
             , C.GOODS_CHARGER_NM goodsChargerNm
             , DECRYPT(C.GOODS_CHARGER_EMAIL,'P008') goodsChargerEmail
             , C.GOODS_CHARGER_CTTPC goodsChargerCttpc
             , DECRYPT(C.GOODS_CHARGER_MBTLNUM,'P007') goodsChargerMbtlnum
          FROM TST_GOOD_INQR_I A , OP_USER B , TST_GOOD_INFO_I C
          WHERE GOODS_INQRY_NO = #{goodsInqryNo}
          AND A.INQR_ID = B.USER_ID
          AND A.GOODS_CODE = C.GOODS_CODE
    </select>
    
    <update id="commentUpdateAction" parameterType="webGoodsQnAVO">
                
        UPDATE TST_GOOD_INQR_I SET
               INQRY_ANSWER_CN = #{inqryAnswerCn}
            <if test='answerSttusCd == "1001"'>
             , ANSWER_STTUS_CD = '1002'
             , ANSWER_DT = SYSDATETIME
            </if>
            <if test='answerSttusCd == "1002"'>
             , ANSWER_UPDT_DT = SYSDATETIME
            </if>
        WHERE GOODS_INQRY_NO = #{goodsInqryNo}

    </update>

</mapper>